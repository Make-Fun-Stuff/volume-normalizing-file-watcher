{
  "name": "mp3-volume-analyzer",
  "version": "1.0.0",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run stylecheck && tsc --pretty",
    "release": "npm run build && npm run test",
    "stylecheck": "prettier --check **/*",
    "format": "prettier --write **/*"
  },
  "keywords": [],
  "author": "Gavin Scott",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.14.6",
    "prettier": "^2.8.4",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  }
}
